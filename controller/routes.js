const router = require('express').Router;

// the routes
const shortUrlController = require('./shortUrl');

router.get('/:id', (shortUrlController.redirect);

        router.post('/url', (shortUrlController.wheel);

            module.exports = router;